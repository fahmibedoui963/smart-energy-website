# Smart Energy website - environment variables
# Copy this file to .env and fill in the real values.
# NEVER commit .env (it is git-ignored).

# PostgreSQL connection string (Supabase) - used at runtime
DATABASE_URL=""

# Direct connection string (Supabase) - used for migrations
DIRECT_URL=""
